# Page vs Window Resolution

This library supports default navigation using both pages (inside a main window with a frame) and windows (each viewmodel associated with its own window) as well as a mix of both. 

